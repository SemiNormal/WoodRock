<UserControl x:Class="WoodRock._UnitDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="92" d:DesignWidth="196" DataContext="{Binding}">
    <Grid>
        <TextBox Height="23" HorizontalAlignment="Left" Margin="18,14,0,0" Name="uxName" VerticalAlignment="Top" Width="120" Text="{Binding}" />
    </Grid>
</UserControl>
